# 个人从者与礼装库构建

任务“构建个人从者礼装库”用于扫描玩家当前账号的仓库，为后续自动编队建立可复用的本地持有数据。

## 使用方式

1. 在主界面启动任务。任务会依次点击“菜单 → 编成 → 英灵一览”，进入灵基一览仓库。
2. 在“构建范围”中选择“从者和礼装”“仅从者”或“仅礼装”。
3. 在“礼装识别范围”中选择“全部 4/5 星礼装”或“仅羁绊礼装”。仅构建从者时该选项不会生效。
4. 扫描期间不要手动滚动或点击游戏画面。

任务只会切换灵基一览中的从者/概念礼装页、设置筛选并滚动，不会选择卡片、保存队伍或开始关卡。完成后会关闭灵基一览与编成菜单，返回主界面。

## 本任务专用的小图标模式

“构建个人从者礼装库”会把从者页和礼装页都切到“小图标”，这是库存构建任务的专用设置，不能照搬到自动编队、强化、礼物盒等任务。1280×720 下每行固定显示 8 张卡，纵向行距约 149px；画面通常有 3 个完整行，并露出第 4 行上半部，因此一次截图可处理 24–32 个候选。

原身份模板不会落盘改图，而是在 `inventory_scan_action.py` 中按小图标比例使用 Bilinear 临时缩放：

- 从者 `servant_face` 从 158×158 缩为 118×118。完整行使用较高信息量的面部区域，底部半行使用较短裁剪；两种特征共享同一身份候选和两帧稳定校验。
- 礼装 `EquipFaces/list` 从 147×56 缩为约 110×42，左缘相对卡片为 11px、上缘为 24px。批量特征只用于筛候选，写入前仍以缩放后的完整模板在 ±5px 邻域做 `TM_CCOEFF_NORMED` 复核。

行列不再依赖头像底栏或首排固定 Y 坐标。扫描器用 `assets/resource/base/image/个人库存/稀有度/rarity1_0.png` 的单星模板检测各列星级图标，要求 `TM_CCORR_NORMED >= 0.90`，再用 8 个固定列和 149px 行距联合求当前 Y 相位。只有能组成至少两行、每行至少 4 列的相位才会接受；底部星级尚未露出的半行由已确认相位外推。这样滚动存在惯性时，首排 Y 可以逐屏重新测量，而不是写死。

稀有度模板由 `tools/generate_inventory_rarity_templates.py` 从解包图生成：仅把纯黑背景替换为 Maa 绿幕，保留星星自身的非零暗色描边，以 Bilinear 缩放到 0.74 倍，并增加 1px 绿幕边。源文件、输出尺寸和 SHA-256 记录在 `tools/manifests/inventory_rarity_templates.json`，可以复现而不修改解包源图；生成记录不放入 `assets/resource`，避免被 Maa pipeline Schema 校验器误判为运行时配置。

## 输出文件

- `config/Inventory/player_servants.json`
- `config/Inventory/player_equips.json`

`config` 是本地配置目录，默认不会提交到 Git。写入采用临时文件加原子替换：只有识别流程确认到达列表底部后，才会替换该类别已有的有效 JSON；中止、超时或识别失败会保留旧文件。

每份 JSON 都包含：

- 数据结构版本、生成时间和实际识别数量；
- 项目内目录总数、已有识图模板数、缺失模板的 ID；
- 每个持有对象的 ID、名称、星级、匹配模板和置信度；
- 从者的职阶与羁绊标签，或礼装的羁绊效果与筛选标签。

礼装图片无法可靠判断是否满破，因此当前版本明确写入 `limit_break: null` 和 `limit_break_known: false`，不会猜测满破状态。

## 完整性的含义

`scan_complete` 表示滚动过程已确认到达当前筛选列表底部。`catalog_complete` 表示工程目录中的每一条数据都有可用于仓库识别的图片模板。两者相互独立；例如扫描可以完整结束，但新礼装尚未补图时，`catalog_complete` 仍为 `false`，对应 ID 会列在 `missing_template_ids` 中。

2026-09-23 起，到底判定同时要求滚动条位于底部、位置稳定及卡片画面稳定，并连续满足 3 轮；仅画面静止或连续识别到相同 ID 不再视为到底。任意一屏零命中时先原位重试一次，仍为零则停止并保留旧文件。列表停住但无法取得底部滚动条证据时也会停止，避免中段卡住被写成完整库存。

因此，没有可靠滚动条的短列表、真正空列表，以及整页均无可识别模板的情况，目前会保守停止。`scan.recognition_valid=true` 仅表示各屏有有效识别且通过上述完成检查，不表示每张卡都被识别；未匹配卡片仍计入 `unmatched_candidate_cells`。

从者和礼装都会先执行一次“打开筛选 → 重置 → 决定”，以清除游戏原有的默认筛选，然后第二次打开各自独立的筛选页设置本任务条件。从者会在 `[108,28,433,617]` 区域内一边小幅下滑一边匹配“从者”选项，排除经验值卡、芙芙等非从者项目。礼装全部模式会选择 4/5 星；仅羁绊模式会把礼装筛选页滑到底，识别“羁绊效果”文字并点击其左侧复选框。识别采用两帧一致性、最低置信度和第一/第二候选分差三重约束。可见但不能可靠匹配的卡片只计入扫描统计，不会写入玩家库。小图标模式下从者最低分/分差为 0.50/0.06：30 屏只读 A/B 表明，相比 0.60/0.10 仅补回 4 个旧快照内已有且当前画面第一候选明确的 ID；礼装批量候选最低分/分差为 0.85/0.15，完整模板复核仍要求 0.88。

## 当前验证状态

- 2026-09-23：新增 6 项库存异常回归，覆盖首屏/后续页零命中、中段静止、缺少滚动条、正常到底，以及失败时不写库存。用 2026-09-21 的 38 屏实机截图和已记录身份候选回放新扫描方法，保留 411 个唯一从者，第 38 屏通过滚动条确认到底（中心 679、连续稳定 3 轮）。此项是历史截图回放，不是当天重新连接设备的完整扫描。下列旧记录中的 `bottom_detection=content` 属于旧算法。

- 2026-09-21 对“小图标”现场截图完成离线回归。稀有度单星锚点在从者三行的逐行均分为 0.9552/0.9564/0.9574，在礼装三行为 0.9638/0.9666/0.9717；六行都是 8 列全部超过 0.90。检测行距均为 149px，并能外推底部第 4 行。
- 同一张从者截图有 32 个候选格，缩放后的身份匹配 32/32 通过，最低分 0.6146，且账号数据中对应从者的等级顺序与画面一致。礼装截图中，目录已有模板的 10 种候选全部通过完整模板复核，分数为 0.9243–0.9911；其余画面新礼装在当前目录中缺少可用模板，没有通过降阈值强行归档。
- 2026-09-21 又完成 30 屏只读全量诊断。旧 0.60/0.10 得到 391 个唯一第一候选，0.50/0.06 得到 395 个；新增的 `102700`、`2800300`、`302400`、`405800` 都在 2026-08-07 账号快照中。单独现场复核中，宫本武藏 `101700` 为第一名 0.8628、分差 0.1739；冲田总司 `102700` 为第一名 0.8761、分差 0.0946，证明冲田的旧版漏识别来自 0.10 分差门槛。当前截图中的红阎魔 `103700` 为第一名 0.8884、分差 0.3674，证明模板与同比裁剪正常；她在旧全量扫描中缺失的原因是整行被大幅惯性跨过。
- 从者列表手势为 `(1204,560) → (1204,310)`、持续 1000ms、等待 1200ms。横坐标固定在用户确认不会打开卡片详情的右侧窄条；旧 400px 手势受惯性影响至少一次跨越约 3.9 行，使某行在上一屏只露底边、下一屏又进入顶栏遮挡区。250px 全量对照扫描 38 屏，相邻 37 次转场全部保留至少一部分已识别行，零重叠由旧方案的 9/29 降为 0/37；行相位仍逐屏重测。
- 同日 250px 手势全量 A/B 扫描得到 411 个符合 0.50/0.06 的唯一第一候选，比 400px 对照多 16 个。该结果说明“固定星级网格解决 Y 相位、缩短手势解决整行覆盖、降低分差解决冲田边缘候选”三项需要同时成立。
- 每轮身份匹配前会直接复用本任务“从者/礼装图标大小检测”的 `整理礼物盒/图标缩放按钮小.png` 模板确认仍在小图标库存列表；不再只依赖页签激活色。若模板在最多 2 秒的切页容错后仍未出现，任务会按一次 Esc，固定等待 1500ms，再在最多 2.5 秒内连续两帧确认模板。只有确认恢复后才继续；详情页画面不会计入匹配、累计数量或到底判断，恢复次数会写入 `scan.detail_recoveries`。
- 同日正式 Custom Action 从灵基一览完成安全窄条全量扫描：39 屏、38 次下滑、411 名唯一从者，`scan_complete=true`、`catalog_complete=true`、`bottom_detection=content`、`detail_recoveries=0`；任务结束后正常关闭灵基一览并返回主界面。宫本武藏、冲田总司和红阎魔均写入最终 JSON，分数分别为 0.8714、0.8760、0.8884。本轮没有自然触发误入详情，因此返回恢复分支已实现并静态校验，但尚无实机触发记录。
- 检查提交 `4871903 feat: 补齐最新从者识别资源与配置` 后确认两处“图片已存在但运行目录未关联”：玛修的新灵衣 `f_8002100.png` 没有并入 `800100.images`；Atlas 中 `1002100` 是 `collectionNo=0` 的芙萝拉内部变体，但其 `f_1002100*` 曾被旧占位条目单独占用，造成与正式芙萝拉同分。修正为把这些跨 ID face 合并回正式从者，并让库存构建排除无图鉴号的内部变体/NPC。38 屏离线重放只新增芙萝拉、玛修两名（411→413），没有移除旧命中；随后从主界面实机重跑得到 35 屏、34 次下滑、413 名，芙萝拉/玛修分别以 0.6819/0.7860 命中 `f_10020003.png`/`f_8002100.png`，完整到底且正常返回主界面。
- 最终结果与 2026-08-07 账号快照的 406 名正式图鉴从者交集为 400 名，当前另有 13 名快照后新增或重新持有。快照内 4–5 星共 348 名，匹配 346 名（99.43%）；未命中的 4 名低星均为 3 星，可能已被分解。剩余高星差异是普勒拉蒂与阿周那〔Alter〕：普勒拉蒂内部变体灵衣 `f_5057300.png` 已补入正式 `505600.images`，阿周那的 4 张灵基和 2 张灵衣也已齐全，但 38 屏离线证据中两者都没有形成接近第一候选的真实卡位。现阶段按账号状态变化处理，不为追平旧快照继续猜图或降低阈值。

- 2026-09-05 已在 1280×720 模拟器上从主界面完成端到端验证。入口“菜单 → 编成 → 灵基一览”、两个仓库页签切换和完成后返回主界面均正常。
- 2026-09-05 的旧“大图标”基线中，从者筛选先“重置 → 决定”，再次打开后在 `[108,28,433,617]` 内每次小幅下滑 90px；第 12 次画面命中“变还_从者未选中”，分数为 0.9955。完整扫描在第 55 屏由滚动条连续稳定 3 次确认到底，写入 370 名唯一从者。该数量仅保留为历史对照，不代表当前小图标实现的最终结果。
- 2026-09-05 的旧“大图标”礼装基线中，“重置 → 决定 → 再次筛选 5 星/4 星”完整通过。扫描固定读取 `EquipFaces/list`，不使用 `team`；完整扫描在第 126 屏确认到底，写入 406 种唯一礼装，直接匹配分数为 0.8863–0.9937。
- 2026-09-05 对仅羁绊模式进行了完整实机扫描：重置与首次决定命中 0.9710/0.9949，“羁绊效果”命中 0.9975，筛选决定命中 0.9949。顶部和底部交叉截图确认游戏实际筛出 4 行、每行 6 张，共 24 种不同卡面。
- 该测试发现旧扫描器用 `feature_y >= 345` 排除了顶部完整行，修正为从 `feature_y >= 160` 开始识别后，成功归档由 14 种提高到 16 种。其余 8 种在当时的 `EquipFaces/list` 中没有可通过完整原图复核的模板（最佳候选约 0.35–0.69，远低于 0.88），因此没有通过降低阈值强行写入；这份 16 种结果只保留作仅羁绊模式的历史基线。
- 2026-09-21 从主界面完成“小图标 + 星级网格”全部 4/5 星礼装实机扫描：68 屏、67 次下滑、402 种唯一礼装，其中四星 136 种、五星 266 种。`scan_complete=true`、`bottom_detection=scrollbar`、`detail_recoveries=0`，完整模板复核分数为 0.8829–0.9911；任务正常关闭灵基一览并返回主界面。目录 1136 条中已有 1108 份模板，缺少 28 份，故本轮 `catalog_complete=false`。
- 与 2026-08-07 账号快照交叉核对：快照内 259 种五星礼装本轮 259/259 全部识别；四星为 128/151。四星差异中的 `英灵极点：BB`、`英灵极点：梅柳齐娜`、`英灵极点：摩根` 明确属于工程缺少 `EquipFaces/list` 模板，其余旧四星可能已被分解，不能只按快照差集判定为漏识别。本轮还识别到快照之后新增或重新持有的 15 种礼装。
- `unmatched_candidate_cells=859` 是逐屏累计诊断量：同一行在 200px 小步滑动下会跨多屏重复出现，同 ID 的多张礼装在单屏结果中也会合并，因此该值包含大量重复格和重复实例，不能解读为“漏了 859 种礼装”。最终库只按礼装 ID 去重。
- 发生超时或识别错误时，先通过 MaaMCP 保留并检查错误瞬间截图，再执行返回或恢复流程。礼装横坐标错误就是按此方式从现场截图定位，避免问题画面被返回主界面覆盖。

## 待继续验证

- 在后续自然出现或可控复现误入详情时，保留现场并确认 `detail_recoveries` 增加、Esc 后小图标模板连续两帧恢复；在此之前不把“恢复分支已实现”等同于“该分支已实机触发”。
- 补齐上述 8 种礼装的 `EquipFaces/list` 模板后，重跑仅羁绊模式，目标是游戏筛选 24 种、JSON 可靠归档 24 种且无重复 ID。
- 按 `FGO整包/解包说明.md` 的仓库礼装裁剪规则补齐当前 28 份 `EquipFaces/list` 缺图，优先复核账号快照中已持有的 3 张“英灵极点”礼装；补图后重跑全部 4/5 星礼装，确认它们进入最终 JSON。旧“大图标”406 种结果没有覆盖列表复位时的顶部第一行，只作历史数量参考。
- 用一个仅有一屏结果的筛选条件验证 `bottom_detection=content`，确保滑动未生效与真实到底不会被混淆；若可稳定识别滚动条，应优先以滚动条到底作为结束证据。
- 分别验证列表中有重复礼装实例、存在新礼装但目录缺模板、以及模板分数刚好位于 0.88 附近的场景，确认输出中的唯一种类数、未匹配格数和告警含义不会混淆。

识图目录的 UI 场景和禁止混用规则见 `docs/识图美术资源目录约定.md`。
